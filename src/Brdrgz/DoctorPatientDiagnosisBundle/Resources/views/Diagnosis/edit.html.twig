{% extends 'BrdrgzDoctorPatientDiagnosisBundle::layout.html.twig' %}

{% block title %}DPD RDBMS - Diagnosis Edit{% endblock %}

{% block content %}
<h1>Diagnosis Edit</h1>

<form action="{{ path('diagnosis_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_errors(edit_form) }}
    
    <div class="form_container">
        <div class="form_row">
            <div class="form_label">{{ form_label(edit_form.name, 'Name') }}</div>
            <div class="form_errors">{{ form_errors(edit_form.name) }}</div>
            <div class="form_input">{{ form_widget(edit_form.name) }}</div>
        </div>
        <div class="form_row">
            <div class="form_label">{{ form_label(edit_form.reference_number, 'Ref. Number') }}</div>
            <div class="form_errors">{{ form_errors(edit_form.reference_number) }}</div>
            <div class="form_input">{{ form_widget(edit_form.reference_number) }}</div>
        </div>
        <div class="form_row">
            <div class="form_label">{{ form_label(edit_form.description, 'Description') }}</div>
            <div class="form_errors">{{ form_errors(edit_form.description) }}</div>
            <div class="form_input">{{ form_widget(edit_form.description) }}</div>
        </div>
    </div>
    
    {{ form_rest(edit_form) }}
    
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('diagnosis') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('diagnosis_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}